
quarkus:
   datasource:
      db-kind: oracle 
      jdbc:
        driver: oracle.jdbc.OracleDriver
        url:  jdbc:oracle:thin:@1.2.3.4:1100NaN/test
      username: said
      password: mahmoud
# drop and create the database at startup (use `update` to only update the schema)
   hibernate-orm:
      database:
         generation: update
